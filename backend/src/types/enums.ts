export enum Difficulty {
  EASY = "easy",
  MEDIUM = "medium",
  HARD = "hard",
}

export enum InterviewMode {
  JOB = "job",
  KNOWLEDGE = "knowledge",
  CV_ONLY = "cv_only",
  MIXED = "mixed",
}

export enum InterviewStatus {
  SETUP = "setup",
  IN_PROGRESS = "in_progress",
  COMPLETED = "completed",
  ABANDONED = "abandoned",
}

export enum InterviewQuestionSource {
  AI_GENERATED = "ai_generated",
  MANUAL = "manual",
}
